import React, { Component } from 'react';
import PropTypes from 'prop-types';

export default class AddProduct extends Component {
    
    //name - text input

    //brand - select

    //price - number input

    //etc - hit all the fields in the products database

    //on submit, make put api call
    
    static propTypes = {

    }

    render = () => null
    
}